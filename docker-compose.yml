services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: tezgahimdan
      POSTGRES_PASSWORD: devsifre123
      POSTGRES_DB: tezgahimdan
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
